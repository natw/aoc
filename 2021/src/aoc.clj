(ns aoc)
; (ns aoc
;   (:require [clojure.string :as string]))

; (defn string-lines [fname]
;   (string/split-lines (slurp (format "inputs/%s" fname))))

; (defn make-ints [things]
;   (map #(Integer. %) things))

; (def int-lines (comp make-ints string-lines))
