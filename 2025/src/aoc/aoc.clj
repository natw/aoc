(ns aoc.aoc
  "FIXME: my new org.corfield.new/scratch project.")

(defn exec
  "Invoke me with clojure -X aoc.aoc/exec"
  [opts]
  (println "exec with" opts))

(defn -main
  "Invoke me with clojure -M -m aoc.aoc"
  [& args]
  (println "-main with" args))
